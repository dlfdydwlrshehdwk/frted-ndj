로드구역만들고

배열만들어주고

과일명이랑 이미지 맞춰주고

대상요소 - 배열출력하는곳 이미지출력하는곳 버튼들 선택박스들 변수화 해주고

기본배열을 배열정보 출력하는곳에 출력해주고 - 글씨나옴

showArr() 바로 호출되게 해주고

이제부터 버튼누를시 이벤트 발생되게 하기 버튼 0~6까지

버튼 0 = 과일주세요 누르면 이벤트실행
얘기능은 누르면 배열 과일 내용을 그림으로 출력되게 해주는거
버튼0 누르면 -> 이미지출력되는곳에 글배열을 출력해줘 html코드로다가
과일배열에다가 forEach써서 html코드 한줄로 배열만큼 돌려줌 

미리 변수에 <ul>만 만들어서 forEach로 li html코드 한줄해서 돌려주고 그 변수에 </ul> 닫아줌 그래서ul li 완성 

과일 뭐있는지 있는공간에 데이터바인딩해주기 
처음부터 다 실행되어있어도 되니까
(()=>{})();   바로실행함수로 작성
코드담을 변수 만들고
for in문으로 과일배열을 싹돌려줌 이미 옵션하나있고 <option/>으로 x만큼 돌려준다.

그리고 과일있어야되는곳에 innerHTMl해줌 


기본셋팅은 되었고 
버튼1부터 기능 구현

기본틀은 다 버튼[x번].누르면 = 함수실행 
1.번버튼
배열뒤에 내용을 추가하는건 push()
배열에 넣어줘(과일모음집의 값을) <- 데이터바인딩해준거 값말하는거
화면에 출력한다.

2번버튼
배열 앞에 추가하는 키워드는 unshift()이다.
배열 앞에 넣어줘(과일모음집값을) 
출력한다.

3번버튼
배열뒤에 삭제하는키워드는 pop() 이다
따로 삭제해야하는 값 자체가 없으니까
배열.pop() 되시겠다.
출력한다.

4번버튼
배열앞에 삭제하는 키워드는 shift이다. shift가 들춰내다 라는뜻도있나보다.
마찬가지로 삭제해야하는 값 자체가 없으므로
배열.shift()하시면 되시겠다.
출력한다.

중간배열을 지우거나 넣기위해 현재 배열번호 선택박스와 지울개수를 요소에 셋팅해준다.
선택할과일 박스 와 몇개?를 적는 박스 를 변수화해준다는 소리

현재 배열값을 선택할과일박스에 셋팅하는 함수를 만들어준다.
함수가 실행될때 현재값은 초기화해주고
과일배열의 값을 option value로 순번을 태그안에 넣어준다.

한번호출해준다.

그리고 이 함수는 배열이 추가되거나 삭제되거나 중간삽입되거나 중간제거를하든 어디에든 넣어야하는 함수이므로
버튼함수들의 출력한다. 밑에 다 넣어준다 
-> 버튼함수들 밑에는 다 출력한다. 호출한다가 다 있으면 되시겠다.

그럼이제 5 6 번 버튼들을 만들준비완료

5번버튼을 누르면 
배열중간을 삭제가능하다 원하는 부분부터 원하는 개수만큼 삭제가 가능
부분삭제 키워드는 splice이다 splice(순번) -> 순번뒤부터 싹다 삭제  splice(순번,개수) -> 순번부터 개수만큼 삭제
우리한테 필요한건 순번,개수 인데 
여기서는 값을 안적거나 숫자가아니면 원하는 부분부터 싹다삭제
그게 아니라면 원하는 부분에서 원하는 개수만큼 삭제 로 작성했다
~ 라면 이기에if문 써준다.
~거나 ~라면? || 써줍니다 
숫자가아니라면? inNaN 써줍니다.

출력한다. 
현재과일모음박스 업데이트 호출한다.

6번버튼
원하는부분에 내가 정한 과일을 삽입한다 는 기능
삽입하는 기능 splice다
but 삽입시 splice(순번,0,넣을값,넣을값,...)이다
고로 splice(원하는곳,0,내가정한과일값) 으로 하면 되시겠다.
출력한다.
과일모음박스 업데이트 호출한다.
