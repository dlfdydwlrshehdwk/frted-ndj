정보 = 객체 

이벤트 대상 button 들 을 변수로 만듬

for문으로 버튼들을 잡음 여러버튼들 ->btns 의 각버튼을 담을 -> 변수 x
ㄴ 클릭이벤트설정  각버튼을 담고있는 변수 -> x.onclick
    ㄴ 클릭된 버튼을 읽음 글자를담을변수 -> btxt = 이벤트변수-> x.innerText
        ㄴ 버튼글자와 같은 객체속성명의 값을 변수에 담음 객체속성명을담을 변수-> data = 객체명[읽은글자를 담고있는 변수 -> btxt]
        
        for in문으로 해당요소에 데이터 넣기 for(변수 in 객체속성을 담은 변수 -> data)

            ㄴ 읽어온 속성명을 비교해 데이터를 셋팅하기
            switch문 사용 
            case를 각각 맞는 장소에 넣어주기 

        if문으로 객체의 속성값이 문구라면? 설정하고 
        ㄴ한글자씩 출력하기위해 for of문 사용
            ㄴ 문구 속성값->data[z]를  담을 변수->txt를 생성

            태그로 싼 새로운 데이터를 넣을 변수를 생성 빈공간으로 -> newtxt

            for문으로 문구데이터를 넣기 
            for of문 을통해 newtxt에 문구데이터를 넣어줌 한글자씩 담김.

                ㄴ 데이터를 알맞는 대상에게 출력.